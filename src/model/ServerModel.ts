export interface CreateServerModel {
    ipAddress: string;
    name: string;
    userId: number;
}

export interface Server {
    id: number;
    ipAddress: string;
    name: string;
    userId: number;
}